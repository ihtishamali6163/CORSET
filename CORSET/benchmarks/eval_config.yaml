description: "CORSET Benchmark Evaluation Suite"

providers:
  - id: anthropic:messages:claude-3-5-sonnet-20241022
    config:
      temperature: 0.0

tests:
  - description: "Email Validator Refactoring Test"
    vars:
      prompt: "Refactor an email validator script to use standard library features and minimal lines without losing syntax validation, domain parsing, or MX checks."
    assert:
      - type: javascript
        value: "output.split('\\n').length < 35"

scenarios:
  - name: "Baseline (Default)"
    config:
      systemPrompt: "You are a helpful software engineering assistant."
  - name: "CORSET Optimized"
    config:
      systemPrompt: "file://../AGENTS.md"